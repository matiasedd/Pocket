/* eslint-disable no-undef */
describe('Class: GetAllUsersController', () => {
  context('Smoke Tests', () => {
    it('should have a handler method', () => {

    });
  });

  describe('Method: handle', () => {
    it('should return an array of users on body and status 200', () => {

    });
  });
});
