/* eslint-disable no-undef */
describe('Validator: getUserTransactionsValidator', () => {
  it('should return an empty body and status 200 when the user exists', () => {

  });

  it("should return a body with message 'Usuário não encontrado' and statusCode 404 when the user does not exist", () => {

  });
});
